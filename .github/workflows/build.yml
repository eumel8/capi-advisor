name: Build (template)

on:
  push:
    tags:
      - "0.0.*"
  release:
    types: [created]

permissions:
  contents: write

jobs:
  call-workflow:
    uses: eumel8/workflow-templates/.github/workflows/build.yaml@0.0.15
    with:
      ldflags: "-X main.version=${{ github.ref_name }}"
