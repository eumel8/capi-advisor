name: Build (template)

on:
  push:
    tags:
    - '*'
  release:
    types: [created]

permissions:
  contents: write

jobs:
  call-workflow:
    uses: eumel8/workflow-templates/.github/workflows/build.yaml@0.0.14
    with:
      ldflags: "-X main.version=${{ github.ref_name }}"
