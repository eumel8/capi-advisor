name: Build (template)

on:
  push:
    tags:
    - '*'
  release:
    types: [created]

env:
  ldflags: "-X main.version=${{ github.ref_name }}"

jobs:
  call-workflow:
    uses: eumel8/workflow-templates/.github/workflows/build.yaml@0.0.13
